{
  "name": "aitms-server",
  "private": true,
  "version": "0.1.0",
  "description": "AI-Powered Telemedicine Management System — backend API gateway (NestJS). Owns users, workflow, reports and permissions; Orthanc owns DICOM pixels, MinIO owns document blobs.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "tsc -p tsconfig.json && node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.4",
    "@nestjs/core": "^10.4.4",
    "@nestjs/platform-express": "^10.4.4",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  }
}
